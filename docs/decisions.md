# Decision Log

Product decisions that are settled. Don't re-litigate without new
information; amend with a dated entry when one changes.

| Date | Decision | Why |
|---|---|---|
| 2026-07 | Offline-first PWA; PowerSync single `records` table; server enforces ALL permissions at the sync choke point | Field has no signal; one enforcement point beats scattered checks |
| 2026-07 | Explosive entry is TOP-DOWN (totals → weight via multiplier) | Matches how blasters actually count product |
| 2026-07 | Filed office copies are WRITE-ONCE; corrections are new versions | Regulatory record integrity; ATF story |
| 2026-07-29 | Fresh work days start with ZERO crew/equipment rows; Copy-from-previous carries them | Matthew: auto-populate created phantom labor entries |
| 2026-07-29 | Users + Crew = one People list; a login is a property of a person; ONE role, ONE deactivate | Matthew: no duplicate lists |
| 2026-07-29 | Drill plans are standalone under the JOB, drilled over multiple days by multiple drillers, imported into the blast report with coverage verification | Mark's actual workflow |
| 2026-08-16 | K factor lives on the SITE (ground truth); jobs inherit; shots can override | The ground doesn't change between jobs |
| 2026-08-16 | Customer → Site → Job hierarchy; job quick-create auto-structures; legacy fields kept as fallback | Mark's world model; zero-migration |
| 2026-08-16 | Adaptive record pages: tabs on wide, one collapsible scroll on phones, per-device override | Matthew: device-dependent preference |
| 2026-08-16 | Addresses are always full field sets (street1/2/city/state/zip) | Matthew |
| 2026-08-16 | Job numbers auto-assigned per-year (26-041), editable; customer PO is a separate field | Matthew |
| 2026-08-16 | Roles are CONFIGURABLE capability bundles; six built-ins seeded to exact legacy behavior; Admin protected by code | Matthew: stop hard-coding permissions |
| 2026-08-17 | Persona-first process: charters + audit in docs/ govern rounds; spec amended before code | Matthew: stop vibe-coding drift |
| 2026-08-17 | No list renders unbounded history; "needs me now" before archives (smart-list pattern) | Matthew: infinite scroll unacceptable |
| 2026-08-17 | Personas split one-per-file (docs/personas/); Matthew approves and amends each as real-people input arrives | Matthew's review workflow |
| 2026-08-17 | Owner = the blasting company's owner(s); full and total access | Matthew |
| 2026-08-17 | Platform Admin is a NEW, SEPARATE persona: Matthew/the vendor, app-level administration above company roles; platform roles must not live in the per-company roles engine | Matthew — future multi-tenant |
| 2026-08-17 | Blaster is a supervisory role (Mark): senior capacity, sees everything, but not always the job lead — big jobs run multiple blasters | Mark via Matthew |
| 2026-08-17 | Blaster default view = ALL crews' days (supervisory visibility confirmed) | follows from the above |
| 2026-08-17 | Driller dispatch: crews self-organize today; dispatch is a wanted PLANNING feature, optional and never blocking the daily flow | Mark via Matthew |
| 2026-08-17 | Office = Evette; compliance and ATF-audit response are her core jobs — audit-response export elevated in her charter | Matthew |
| 2026-08-17 | Record lifecycle must be a consistent, intuitive pattern; deletes are LOGICAL, preserving a full record of all activity — see deletion-pattern.md (proposal pending approval) | Matthew |
| 2026-08-17 | Blaster workflow validated (see personas/blaster.md): blasters may set up customer/site/job themselves; drill plan carries design intent with a readiness-review/adjust step after drilling; per-hole hazards must inform loading; merged completed-plan view with driller attribution; compliance flags must explain why; seismo readings are point-in-time tied to the blast moment, late attachment normal; each crew member files their OWN daily time card; multi-blaster days = log per responsible blaster (model TBD) | Mark via Matthew |
| 2026-08-17 | Per-person time cards CONFIRMED; Evette approves BOTH the day and the cards | Matthew |
| 2026-08-17 | Multi-blaster model = (a): ONE log per day, each SHOT carries a responsible blaster + their signature | Matthew |
| 2026-08-17 | No job-lead designation — per-shot responsibility covers it | Matthew |

| 2026-08-17 | Days are NOUNS, never verbs: no "start/resume a day" anywhere — verbs act on work and jobs ("Continue — loading Shot 2", "Start work at another job"); the day record assembles itself and appears only with a date on it. Structure unchanged | Matthew |

| 2026-08-17 | Blaster screen study APPROVED (artifact 9bfdcfb9): phase spine confirmed; KPIs dropped from home (specific KPIs may return later); blaster MAY enter a no-login person's time card but ideally shouldn't have to — push toward self-service logins | Matthew |

| 2026-08-17 | Driller workflow validated (see personas/driller.md): grid split stays fluid (no assignment); rig checklist is a STANDALONE daily artifact per rig (paper form Rev.2 mirrored; 50-hr due-state to be computed from hours), drill log only selects the rig; plan general notes suffice; batch hole entry + plan deviations are STANDARD operation (app records the collars physically placed); drill-from-plan is THE model, no-plan path retires; driller daily trio = hours + log + checklist | Mark via Matthew |

| 2026-08-17 | Driller can submit a daily report WITHOUT a blaster when necessary — drill-only days never require blast-side involvement | Matthew |

| 2026-08-17 | Driller screen study APPROVED (artifact cab6eae6): trio home, grid-select batch logging, 50-hour clock advisory-only | Matthew |

| 2026-08-17 | Shop validated: DEDICATED shop; hours become a sourced LEDGER (field entries + shop CORRECTION override — append-only, both values kept, audited); Where's-my-equipment screen derived from filed paperwork (last record → job → site) + manual "at the yard" gesture, no GPS; PM designed on FLAGGED ASSUMPTIONS pending shop-crew verification | Matthew |

| 2026-08-17 | Shop screens revised per Matthew: shop TRIO tiles (Down/Tickets/Due) over one merged worklist; queue order = editable by shop (default downs-first-oldest; drag to reorder — downtime cost / job criticality are shop knowledge); where's-my-equipment gets a MAP (site coords, one-time geocode, offline after; list+map on wide, toggle on phone); shop form factor = landscape tablet/computer first; ledger + PM sections approved as designed | Matthew |

| 2026-08-17 | Deletion pattern APPROVED: verbs = Archive / Delete; hierarchy records deletable only if NEVER used (else archive-only); no extra ATF lock (immutable filings + draft-only delete + permanent server-side audit history suffice) | Matthew |

| 2026-08-17 | Round 1 shipped (0fb2d10, harness31 21/21). Implementation calls made in-round: time-card approval rides `approve_days` (one grant approves the day AND its cards); a FILED card freezes like a filed day — the owner pulls it back (filed→draft) to fix, approved cards freeze entirely, delete is draft-only; NON-approvers may enter cards only for NO-LOGIN roster people (server-enforced — "discouraged" is a hard rule below supervisor); hour corrections are append-only (`correct_hours`: mechanic+supervisor) and stamp equipment.hourMeter as the cached current value; the per-shot guard protects signing AS someone else — any blaster may still claim responsibility for an unsigned shot | follows from approved designs; recorded for future rounds |

| 2026-08-17 | Round 2 shipped (64d6618, harness32 20/20). In-round calls: office send-back now carries a REASON (`blastDay.sendBackNote`, stamped by the status route, cleared on any forward transition/resubmit — the needs-attention strip shows it inline and ranks sent-back above everything); readiness review stored ON the blast log (`readinessReview` object, no new table) and its max-lbs/delay seeds shots that haven't overridden; seismo phase chip is "later ok" — late attach stays policy, not warning; hub is a view of the existing day page (?view= deep-links), not a new route | follows from approved study; recorded for future rounds |

| 2026-08-17 | Round 3 shipped (aeb5c74, harness33 16/16). In-round calls: SKIPPED plan holes are a first-class marker (`DrillLogHole.skipped`) — intent, not drilling: excluded from counts/footage/deviations, shown dashed in the blaster's merged review, and a skipped position is not "undrilled"; the driller's time card can exist BEFORE any work day (cards bind to job+date; the day attaches later); the 50-hour clock reads the HOUR LEDGER (not the bare registry number) against the last weeklyDone checklist; drilling-today prefers the most-active log so stale zero-hole logs can't shadow the live pattern; driller StartGrid drill-log tile removed (it silently created a planless day+shot+log) | follows from approved study; recorded for future rounds |

| 2026-08-17 | Round 4 shipped (caaba3d, harness34 15/15). In-round calls: the shop's worklist ORDER lives on the bench device (localStorage), not synced — one shop, one screen; revisit if the shop multi-devices. Site geocodes save to `Site.geo` only from devices holding sites-PATCH (admin/blaster) and cache locally everywhere else — mechanics get offline pins without widening write grants. PM history lives ON the equipment record (`services[]`, PATCH open to shop) — no new table, no server change. "At the yard" = `equipment.atYardAt`; any field record dated on/after it wins. The whole round is web-only (no shared/server changes) | follows from approved study; recorded for future rounds |

| 2026-08-18 | Role-specific navigation APPROVED (study artifact 5f52a919): one rail rhythm, per-persona nouns. Matthew's calls: the home stays **"Dashboard"** (NOT "Today" — the home carries more than today); field & shop rails re-cut NOW, the admin console fold-in DEFERRED to the Admin round; driller gets a thin **Drilling** page (open plans · assigned · ready-to-drill). Shop rail = Shop · Fleet (equipment registry promoted out of Admin — mechanic's Admin tab retires) · Locator · Records. Office rail provisional (Dashboard · Records · Jobs · Incidents) pending Evette's walkthrough. Rails keyed by homeDashboard bucket so custom roles inherit sensibly; phone tab bar mirrors each rail's top four | Matthew, nav study reaction |

| 2026-09-06 | Soft-launch plan APPROVED (docs/soft-launch-plan.md, review artifact 06afbdee): rounds S1 onboarding/access → S3 feedback+diagnostics → S2 guidance → S4 clutter → S5 gates, all 26 items Build. Calls: cohort = **everyone incl. shop**; feedback goes to **Matthew only** (not visible to company admin); feedback **screenshot on by default**; **some iOS** devices in the cohort — install guidance must cover Safari; **everyone gets an email address first** (email is the reset/notify channel; SMS/2FA wanted if feasible — evaluate in S1, not blocking); profile completion = **soft nag, hard stop at signing**; **office queue home built now** (provisional until Evette's walkthrough). Tours must be revisited for relevance and usefulness, not merely made role-aware | Matthew, review artifact |

| 2026-09-06 | Round S1 shipped (harness37 24/24). In-round calls: the first-run welcome is per ACCOUNT (`User.onboardedAt`), not per device; a temp password set by an admin (reset or Create login) always forces a change at the next sign-in; the profile-completion hard stop applies only to LICENSED roles (blaster/supervisor/admin) at the moment of signing — drillers get the signature nag only; install guidance is dismissible for 30 days per device; forgot-password never reveals whether an email has an account, but does say when the server cannot email at all | follows from approved plan; recorded for future rounds |

| 2026-09-06 | Round S3 design amended before build (soft-launch-plan.md S3): feedback is a **server-side table with a device-local offline outbox**, NOT a synced `records` row — a synced table would replicate every report to every company device, contradicting Q2 ("Matthew only, not visible to the company admin"). Triage is gated by a **platform-admin marker** (`PLATFORM_ADMIN_EMAILS` env, fallback `ADMIN_EMAIL`), the first concrete platform-actor concept, deliberately OUTSIDE the company roles/capability engine per platform-admin.md. Screenshot on by default (Q3) is stored in the row (JPEG ≤1280px), not R2. Error boundary is the one class component in the codebase (React requires it). Async errors: one toast per minute, never a modal | reconciles two approved statements; flagged for Matthew at check-off |

| 2026-09-06 | Round S3 shipped (harness38 38/38; harness37 24/24 regression). In-round calls: html2canvas is lazy-loaded on first composer open (it slowed the dev boot enough to trip S1's fixed-delay checks; the prod bundle only shrinks once pdf.ts stops importing it statically — a perf-pass item); a report the server rejects with a non-401 4xx is DROPPED from the device outbox rather than retried forever; the email fires once per report id (retries are duplicates); the crash screen skips the screenshot (it would only show the crash screen); the `platformAdmin` flag rides the session payload and is refreshed at sign-in, not live — existing prod sessions need one sign-out/in to see the tab | follows from amended S3 design; recorded for future rounds |

| 2026-09-06 | Round S2 started, design amended first (soft-launch-plan.md S2): `tourDoneAt` on the User (per account, mirrors `onboardedAt`) so S2 is Railway-first; tour rebuilt per role bucket around real screens with navigation; coach sheet reachable from the ? menu everywhere ("About this screen"); first-week checklist self-ticks from data, dismissed per device; copy drafted for Matthew's review at check-off. Matthew asked "can you start S2 now" — nothing blocking beyond the Resend key | Matthew (go-ahead) + default calls, flagged |

| 2026-09-06 | Round S2 shipped (harness39 34/34; S1/S3 regressions green). In-round calls: coach sheet lives ONLY behind the ? menu (no per-page header buttons — no shared header exists and one consistent door is easier to teach); the tour auto-run is suppressed by the legacy `shotlog-tour-done` localStorage key too (dev/harness affordance, harmless in prod); Skip counts as done (nagging a skipper is worse than a missed tour — it stays one tap away); first-week manual ticks and Hide are per DEVICE (no server field — cheap to revisit); the walkthrough card pins to the bottom edge when its target is taller than the viewport | follows from amended S2 design; recorded for future rounds |

| 2026-09-06 | Round S4 shipped (harness40 46/46; S1/S2/S3 regressions green; audit sweep max 2.1 screens). In-round calls: **office home** serves the office BUCKET, the admin role keeps the Company view (costing windowed 10) — one bucket, two homes by role; "sent back, waiting" = draft day + `sendBackNote` (the only marker); expiring window is **90 days** (the study's number, not the plan's 30); "Approve all" time cards shows only to roles holding approve_days (office does not by default); the **operation chip's "company default"** is the mode of the company's jobs (no settings field exists); records manager previews PDFs in the browser's own viewer via iframe (no pdf.js dependency — R-B can swap it if iOS first-page-only bites); **Print** opens the selected PDFs in tabs (capped at 6) rather than merging; DocList stays for job/person pages only; walkthrough auto-run is home-only | follows from the approved study; recorded for future rounds |

| 2026-09-06 | Round S5 started (launch gates). Done: prod audit 12 → 4 via in-range updates (react-router, postcss, uuid, qs, picomatch); the 4 left are transitive with no upstream fix (dompurify via jspdf 4.2.1 = latest; deepmerge-ts via Prisma 6 CLI config, deploy-time only) — **accepted for the soft launch, re-check monthly**; engineer review packet written (docs/usbm-curve-signoff.md); runbook written (docs/soft-launch-runbook.md); Reference page no longer claims state/local limits are active (they are planning-side only, and the MA citation is suspect). Findings for the engineer: the seismo badge never uses the plaster curve (structure type is not chosen per shot); air overpressure is unchecked | Claude; decisions D1–D4 below are Matthew's |

| 2026-09-07 | **Rehearsal mode (Round S6) accepted.** Matthew rejected the sandbox-tenant + plus-address + reset-switch plan as too much ceremony for something he will repeat regularly; accepted the one-switch version: platform admin picks a role → signed into a sandbox company as a fresh person of that role → PIN, welcome, tour, empty home → *End* wipes the sandbox and restores his session. Sandbox starts **empty**, with an *Add sample job* button on the bar (his call on the recommendation). Invite email + install prompt are tested once for real, not rehearsed. Resend went live in prod the same day (shotlog.evilgenius.io) | Matthew |

| 2026-09-07 | Round S6 shipped (harness41 22/22; S2/S3 regressions green). In-round calls: rehearsal accounts live in the sandbox with random unusable passwords — sessions are minted by the platform route, never by login; every Start wipes AND resets all six accounts (not just the chosen role) so any order of rehearsals starts fresh; **End may be called by any sandbox session** (only rehearsal accounts exist there); sandbox feedback is stored, never emailed; the client restores the exact device state it stashed (session, PIN, view-as, dismissals, feedback outbox). Platform routes live under `/platform/*`, gated by the platform-admin marker | follows from the accepted design |

| 2026-09-07 | **Prod incident after S6:** Matthew's browser showed "Can't reach server — retrying" with "Last synced: never" and an empty dashboard; a private window synced fine. Cause: the rehearsal switch raced the local-replica clear against an 8-second timeout and reloaded anyway; a half-cleared replica then "connected" for ten hours without ever reaching a checkpoint. Fix (harness41 24/24): `resetLocalReplica()` waits for the SDK clear (30 s), then closes the instance and deletes the local database outright if the clear did not finish — never a partial state; rehearsal Start/End use it; the sync panel gains **Reset local data** (confirms when unsent changes exist) and says "taking too long? reset" after a minute connected without a first checkpoint. **Correction the same hour:** the in-page delete cannot work — the SDK's shared worker holds `shotlog.db` open and IndexedDB defers the delete silently, and Sign out awaited the same wedged clear (Matthew: "can't log out when not connected"). Final fix: `resetLocalReplica()` sets `shotlog-replica-reset-pending` when the SDK clear does not finish, and `runPendingReplicaReset()` in main.tsx deletes the database at the NEXT BOOT before anything opens it; Sign out is bounded and reloads. Device remedy when a build predates this: clear the site's data in the browser | Claude, verified with Matthew |

| 2026-09-07 | **Rig checklist: the rig is a question on the form, not a dashboard setting** (plan artifact f26be2bb; Matthew: Change it → always start empty, quick picks for today's log / last filed / usual / recent + full fleet with search). Home tile is the verb; rig line, Change rig and the picker modal are gone; `/drill-checklist` route with optional rig; usual rig written only on file / on logging with a rig / Settings | Matthew, Claude |
| 2026-09-07 | **Admin › People: Add person in one shot** (mockup artifact 5880eda4, Matthew: build it · sort by last name · Invite default · bulk emails now): one panel with First/Last/Role/Email/Access, self-describing button, offline-honest, duplicate guard; `crewMembers.lastName` (derived for old rows); Paste list invites by email. ⋯ row unchanged for existing people. Server fix found by the harness: creating a login for a roster row the client had not yet uploaded made a DUPLICATE roster row (random id) — the server now creates it under the id the client named, so the upload merges | Matthew, Claude |
| 2026-09-07 | **CI fixed**: the GitHub Actions check had failed on every push since the Prisma client became a typecheck dependency — the runner never ran `prisma generate`, so every Prisma type was `any`. Workflow now generates the client after `npm ci`. Railway/Vercel deploys were never affected (they build their own way) | Claude |
| 2026-09-07 | **S7 follow-up (driller rehearsal)**: (1) driller bucket never prefilled into a blasting type; day page lands on the daily report for drillers even with a blast log; (2) Copy from previous defaults to blank (overrules the S7b "most recent day" default); (3) rig line under the trio with Change rig, Change rig on the checklist header, drill-log rig pick → usual rig; (4) the drill log's back arrow sends a driller home, not to the blaster's plan/hub | Matthew (notes), Claude |
| 2026-09-07 | **S7d shipped** (harness45 25/25; 40/42/44 green): day = container with `authorUserId/Bucket`; blaster takes over on blast-log add; report sections follow the owner; Work Force = time cards by job+date (typed editor retired, legacy rows read-only, "no card yet" list); `DrillLog.endingHours` at Mark Complete → ledger source `drill_log` → derived drill rows on the report; driller card pre-proposed from checklist/log timestamps; dialog offers *Open that day*; two offline copies merge (`mergeDays`). **Permission change:** `blastDays DELETE` = REPORT FAMILY (was registry) in matrix + bundles so the day's own people can fold a duplicate away; server still refuses unless draft + nothing filed. Round S7 complete | Claude, per S7 |
| 2026-09-07 | **S7c shipped** (harness44 31/31; 39 + 41 green): screen tours on the day hub → shot, drill log, rig checklist, shop, approvals, people — auto-run once per account per screen (`User.toursDone`), for everyone in that bucket, never on top of the walkthrough or within 60 s of another tour; re-run from ? → "Show me …" | Claude, per S7 |
| 2026-09-07 | **S7b shipped** (harness43 34/34; 37/38/39/40 regressions green): day dialog Name → Recent → Customer → Site → Job → Date → Type → Copy with lone-site/lone-job auto-fill and type prefill (last day → `Job.defaultTypeOfWork` → device → role); one `NewJobForm` (Customer → Site → Job) for dialog + Jobs lens; lens tabs Customers · Sites · Jobs; Log out only in My Profile (with the replica reset); Settings You · Preferences · Help · Install · Rehearse · Data & device; Records opens on the bucket's paper with four new kinds (time cards, repair tickets, services, hour corrections) | Claude, per S7 |
| 2026-09-07 | **S7a shipped** (harness42 27/27, harness41 25/25): rehearsal Start copies the platform admin's company (fleet, roster without logins, catalog, manufacturers, settings, roles) unless *Start empty*; *Add sample data* loads a connected week (two jobs, plan half drilled, yesterday submitted with three filed cards, today's draft, failed checklist → ticket + rig in shop, rig with 50-h and engine PM overdue, open incident, expiring permit/COI); driller checklist door on the Drilling tab, usual rig on the account (`equipment.assignedUserId`), optional job attach. Source company proven unwritten by the harness | Claude, per S7 |
| 2026-09-07 | **Round S7 accepted** — all eleven items Build. Item 10 on its second draft: role ownership (day = container; blaster owns the report; each person owns their trio; Work Force = cards by job+date; rig hours from checklist start + end-of-day meter at log sign-complete; driller in/out proposed from timestamps and confirmed). Matthew's calls: lens tabs Customers · Sites · Jobs; dialog auto-fills a lone site/job; job-level default type of work; screen tours once for everyone; shop files no cards yet; nudge only; "take over the report" tabled. **S7a started** (rehearsal data clone, whole-workflow sample, checklist doors) | Matthew |
| 2026-09-07 | **Round S7 proposed** from Matthew's first-rehearsal feedback (11 notes) — docs/round-s7-plan.md + review artifact "Round S7 Plan". Grouped into S7a rehearsal data/sample/checklist door, S7b hierarchy + day dialog + settings + log-out + records-by-bucket, S7c screen tours, S7d time & day ownership ("your hours are yours; the day belongs to whoever started it; Work Force is a roll-up"). Nothing built until he reacts | Claude, proposal |
| 2026-09-07 | **Brand mark adopted** — the final-kit logo (mushroom cloud + shockwave on a navy #1C3859 tile, orange #EE7A2E "LOG") is the one ShotLog mark: favicon, PWA/home-screen icons (192/512 + maskable), apple-touch-icon, sidebar and mobile header, sign-in / enroll / reset frames, install card, invite-email header. One in-app component (`components/brand/ShotLogLogo.tsx`); static copies in `apps/web/public/`. Tailwind `navy`/`safety-orange` moved to the kit values. PDFs keep the customer's company header, not the ShotLog mark | Matthew (kit in Downloads/shotlog-final-kit) |
| 2026-09-07 | **Unasked, you see only what you authored; everything else you go and get.** Supersedes "blaster default view = ALL crews' days" (2026-08-17). Field homes (blaster, driller) surface only the person's own work — a day is *mine* when I authored it or hold a time card on it; drilling waits on me when I laid the plan (plans are blast-family-only, so nothing is unowned). `/days` = Mine / Everyone switch, default Mine, per device; Records › Company is the review door. Office, admin, shop see everything. Sync and permissions unchanged — presentation only. Pre-stamp days (no author) live under Everyone. PIN is per account per device; sign-out and enroll clear it | Matthew (invite test; plan artifact ed13a981, all four calls Build) |
| 2026-09-07 | **Filed PDFs live in file storage (R2), never in the synced record; the device keeps the company's copy across sign-out.** Matthew's phone: 567 records took 23.6 s because 23 pre-Jul-29 filings carried ~490 KB PDFs inline (7.6 of 8.4 MB); identical times on IndexedDB and OPFS proved the engine irrelevant. Server boot job `legacyPdfs.ts` moves inline PDFs (and frozen asset copies) to R2 under the app's own key scheme and rewrites the record (pointer in, bytes out, checksum kept); `/health.legacyInlinePdfs` watches it reach zero; `/health.files` says whether R2 is configured. Sign Out keeps the replica (tagged with its company id); a different company's sign-in clears it; Profile gains "Sign out & clear this device". The sync chip never says Synced while `hasSynced === false` (shows download %). Storage-engine selector removed from Settings; timing line stays in the sync log | Matthew (plan artifact 742b52b4: R2 already in use for attachments; keep copy; PDFs fetched on open only) |
| 2026-09-07 | **Apple WebKit devices default to OPFS storage; everything else stays on IndexedDB.** After the PDF migration Matthew's iPhone applied the same 567 records in 25.3 s through IndexedDB and 1.4 s through OPFS (the pre-migration "identical" readings were the 8 MB download and the slow writes overlapping). Desktop Chromium is faster on IndexedDB (0.6 vs 1.3 s). Policy is a pure, unit-tested rule (`lib/storageEnginePolicy.ts`); a fresh Apple device starts on OPFS if the browser can open it, a device already holding an IndexedDB copy hands over at its next launch only once its upload queue is empty (no local write can be lost), and any browser that cannot open OPFS falls back to IndexedDB with a sync-log line | Matthew's second phone reading (fresh PWA install) |
| 2026-09-07 | **Round S8 (Matthew's eleven; plan artifact 8d55ab6c, three drafts).** Accepted: (1) invite email in *testing mode* — invitation, not instructions; feedback via the app only; (4) load/soak test is a MANUAL overnight GitHub Action against a staging copy, never during development, never on production; (5) connectivity probe every 10 min with email on failure/recovery; (6) driller empty state names the blaster; (8) feedback screenshot viewer; (9) Equipment tabs by type, repair queue OUT of the admin page; (10) Start-work dialog = Option B: Name first, one Job row opening a full-height Customer › Site › Job picker with search + recent, pinned Start; (11) drill plan first with a pinned Send footer, Continue that knows plan/send/drilling/review, timing built from the drilled pattern; (7) Jobs = drill-down (nav label stays *Jobs*; details above the lists; windowed lists; wide-screen mock before build); (3) Alpha / Beta / Production companies with a platform-admin switcher (build). Deferred: (2) help guide until the UI settles. Order S8a → S8b → S8c → S8d | Matthew |
| 2026-09-07 | **Round S8b calls (plan artifact e4e0cbad, two drafts, tap-through).** Jobs = Option 1 drill-down: the nav item stays *Jobs* and lands on **Customers**; a customer's page shows its About cards (Company & billing · Contacts · Compliance) FIRST, then its Sites; a site's page shows Ground · Permits · Access · Contacts first, then its Jobs; the job page is unchanged. Wide screens use the SAME pages with the About cards across the top and the list below (**Wide 1 · Pages** — a three-column layout was offered and not chosen). The flat "All jobs" list, the Customers · Sites · Jobs switch and the sidebar sub-items are **dropped**: recent-job chips plus one search that finds customers, sites and jobs (name, town, job number) replace them. Every list is windowed to 15 with Show all. **Equipment:** four grouped tabs — Drilling · Trucks & trailers · Machines · Blast gear (grouping B over his own Drills · Trucks · Miscellaneous: nothing is "miscellaneous", and the seismograph's calibration sits with the mats, not next to a crusher) — type chips inside a tab, search across groups, stacking filter chips (Active · In shop · Retired · Repair open · Out of service · Due ≤30 d) whose counts follow; the repair queue leaves the admin page and stays on the shop home. | Matthew, S8b reactions: wide = Pages; All jobs = drop; equipment first draft = Change ("group the types… search and filter… filter by repair status so the office can drop the list"); second draft = grouping B + Build. |
| 2026-09-07 | **Round S8c — Alpha / Beta / Production companies (plan artifact 57e5b772; screens Build, calls accepted with one change).** Companies carry an **environment** (alpha · beta · production · sandbox). Naming per Matthew: **"Baystate Blasting (Alpha)" / "(Beta)"** — the tag after the name, never before. A platform admin switches companies from Settings › Company (platform admin only): the account gets a hidden admin **twin** in each company it enters (`User.platformRootId`; email = root's with a `+c-<id>` tag; no password; sessions minted only by the switch; the twin inherits the platform-admin marker), the device's PIN carries over to the twin, the twin is born onboarded (no welcome/tour), the previous company's replica is cleared and the new one downloads. Admin › Companies (platform tab): list with environment/people/records, **New company** (name · environment · start from a company's reference data = equipment, roster without logins, catalog, manufacturers, settings, roles — or empty), **Rename** (also rewrites the synced companySettings name), **Move people here** (the person's account moves — email, password, role, licenses, signature, PIN on their devices — the roster entry is re-linked in the target, their old records stay behind, their refresh tokens are revoked so each device signs in once), **Delete** for non-production companies only with a typed name. Invites go to the company of the session and the mail follows the company's environment (alpha/beta → testing invitation, production → the real one; `INVITE_MODE` env stays as an override). Twins are hidden from People and from Move lists. | Matthew's S8 item 3 ("Alpha Baystate Blasting for alpha testing, Beta for the testers I'm about to onboard, production Baystate Blasting once we go live"); rehearsal's sandbox mechanics reused; the platform-admin charter (platform roles outside the company roles engine). |
| 2026-09-08 | **Round S8d — the load test runs against the NAMED API (production by default) inside a throwaway Beta company, not against a staging deployment.** There is no staging copy of the stack; standing one up is infra + cost for a test that runs a few nights a year. S8c's companies give the isolation that matters — its own sync bucket, deleted at the end — and the workflow is manual only, so the load lands when nobody is working. A true staging URL, if ever built, is one input away. The probe's memory is a GitHub issue (`probe-down`), so a scheduled job with no state still sends exactly one DOWN and one RECOVERED email. Signed-in checks read *skipped* until the probe secrets exist, so the schedule never cries wolf on day one. | Matthew's S8 calls (4: manual overnight, never during dev; 5: every 10 min, email on fail + recover); no staging infra exists. |

## Open (waiting on Mark / Matthew)

- **S7 item 10 second draft** (artifact id `i10b`): role-based ownership (day = container, blaster owns the report, each person owns their trio, Work Force keyed by job+date, rig hours from checklist start + new end-of-day meter at log sign-complete, driller card pre-proposed from timestamps). Questions: meter prompt placement, suggestion vs silent fill, "Take over" now or later.
- ~~S7 reactions~~ → received 2026-09-07 PM: items 1–9, 11 Build (lens tabs reorder; day dialog auto-fills lone site/job; job-level default type of work; tours once for everyone); item 10 first draft rejected — shop no cards, nudge only, driller hours auto-populated where possible.

- **S5 decisions (runbook §5):** D1 test users (recommend: keep dev as the test bed); D2 who reviews the USBM packet + whether badges get an "advisory" label until signed (recommend: yes, one-hour change); D3 cohort order (recommend staggered); D4 weekly cadence (recommend Mark + Evette, 30 min).

- **Office queue + records labels (S4):** section titles and status labels are mine — read the office home and Records once and mark what rings false. Evette's walkthrough still governs the office home.

- **Tour + coach copy review (S2):** drafted from the charters in the DrillingWork voice, not from crew interviews — read `components/guidance/coach.ts` and `tourScripts.ts` once and mark what rings false.

- **Feedback ownership (S3 in-round reconciliation):** S3 shipped feedback as platform-admin-only per Q2. If Mark should ALSO see a company-level suggestion box, that is a new capability + a company-scoped view — say so and it becomes an S4/S5 item.

- **Jobs-card chips (I4, "talk first"):** which chips stay on the job row under the Active filter — settle before S4.4.
- **Records density (L6, "talk first"):** no fix proposed for the 2.1-screen Filed lens; Matthew wants to discuss what bothers him there.
- **SMS or two-factor for password reset (Q5 note):** SMS needs a provider (Twilio) + US A2P registration (weeks, per-message cost); authenticator-app 2FA needs no provider. Decide in S1 whether either is worth the field friction on top of the device PIN.

- **Test-user cleanup (Round 5 decision point):** prod has only Matthew's real
  admin; the LOCAL dev DB carries test logins (blaster@/mechanic@/office@/
  dinis@test.local + stray foreman accounts) and harness residue. Decide
  before real crew onboarding: wipe the dev company data, or keep it as the
  permanent test bed and never point a real device at it.

- Permits-on-site placement + job status names (quoted/active/on_hold/complete) — provisional
- Owner: quote structure; who may see dollars (`view_financials`); does Owner write or only read
- Shop-crew verification (held for later): real PM intervals + shop paper artifacts, parts capture, bench-vs-truck
- Persona charters: blaster + driller + shop APPROVED (shop pending crew verification); office/owner/admin/platform-admin awaiting
